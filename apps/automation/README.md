# Automation App

Reserved for future workflow workers.

The current backend exposes an `AutomationModule` and event bus hooks. Long-running workflows, scheduled jobs, and external API orchestration can be extracted here later without changing the Home Assistant -> Backend -> LLM flow.

